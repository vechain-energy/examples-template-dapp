The following libraries were optionally added and can be replaced by others:

1. antd @ant-design/icons for UI
1. react-router-dom for routing
1. dotenv-webpack to support environment based configuration
